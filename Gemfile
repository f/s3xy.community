source "https://rubygems.org"

# Simple Jekyll setup that works with GitHub Pages
gem "jekyll", "~> 4.3.2"
gem "webrick", "~> 1.8"  # Required for Ruby 3.0+

# Optional plugins (GitHub Pages will use safe mode anyway)
group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
end
